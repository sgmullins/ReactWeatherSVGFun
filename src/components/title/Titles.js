import React from 'react';
import { MainTitle, Subtitle } from './title-styles';

const Titles = () => (
  <div style={{ color: '#4db8c3', position: 'absolute' }}>
    <MainTitle>Weather Finder</MainTitle>
    <Subtitle>Find out weather and more...</Subtitle>
  </div>
);

export default Titles;
